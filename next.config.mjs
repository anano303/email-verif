// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true, // თუ App Router-ს იყენებთ
  },
  // სხვა კონფიგურაცია...
};

export default nextConfig;
